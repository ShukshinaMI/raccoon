import { Space } from "antd";

const EmptyRegistry = () => {
  return (
    <Space style={{ justifyContent: "center", marginTop: "20px", color: "rgba(54, 58, 64, 0.88)" }}>
      По вашему запросу ничего не найдено
    </Space>
  );
};

export default EmptyRegistry;
